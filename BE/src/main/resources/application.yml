spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://144.24.73.155:3306/kxam?characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: admin
    password: wlrdnjsqudehd

  jwt:
    secret: DvqcGn8mnFjqSL4a

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true #To beautify or pretty print the SQL
        show_sql: false #show sql

server:
  # 프로젝트 환경의 포트 설정
  port: 8185
  domain: localhost
  servlet:
    session:
      timeout: 1440m

logging:
  level:
    org.hibernate:
      type.descriptor.sql: trace #show parameter binding
      SQL: DEBUG